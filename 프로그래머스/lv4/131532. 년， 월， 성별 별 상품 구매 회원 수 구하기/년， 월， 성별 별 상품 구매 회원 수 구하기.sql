-- 코드를 입력하세요
SELECT YEAR(SALES_DATE) year, MONTH(SALES_DATE) month, GENDER, COUNT(DISTINCT a.USER_ID) FROM ONLINE_SALE a LEFT JOIN USER_INFO b ON a.USER_ID = b.USER_ID WHERE GENDER IN (0,1) GROUP BY year, month, GENDER ORDER BY year, month, GENDER;